<?xml version="1.0" encoding="utf-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
           xmlns:order="orderNamespace"
           elementFormDefault="qualified">

    <xs:import namespace="orderNamespace" schemaLocation="order.xsd"/>

    <xs:element name="library">
        <xs:complexType>
            <xs:sequence>
                <xs:element name="order" type="order:orderType"/>
            </xs:sequence>
        </xs:complexType>
    </xs:element>

</xs:schema>